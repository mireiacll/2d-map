// const IS_DEV = import.meta.env.DEV;

// // Detect the current hostname automatically
// const currentHost = window.location.hostname; 

// export const GEOSERVER_BASE = IS_DEV
//   ? '/geoserver'
//   : `http://${currentHost}:8080/geoserver`;

export const GEOSERVER_BASE = '/geoserver';